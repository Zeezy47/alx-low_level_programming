#include "main.h"

/**
 * print_line - function that draws a straight line in the terminal
 * @n: input number
 * Return: Always 0
 */
void print_line(int n)
{
	int i;

	if (n > 0)
	{
		for (1 = 0; 1 < n; i++)
			{
				_putchar(95);
			}
			_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
